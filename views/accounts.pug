h2 Plugin Info
br
h5.text-muted Overview
.table-responsive
  table.table.table-striped.table-sm
    thead
      tr
        th Field
        th Value
    tbody
      tr
        td My Address
        td #{address}
      tr
        td # Plugins
        td #{Object.keys(accounts).length}

